1) Install Qt SDK. Go to http://qt.nokia.com/ and download the latest version
   (project created with Qt SDK 1.2).
2) To compile the project in directory TASK_MANAGER run:
   qmake
   make
3) To compile unit tests in directory unit_tests run:
   qmake
   make

All the files encoded using UTF-8.
